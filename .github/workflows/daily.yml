name: Thesis Pulse Daily

on:
  schedule:
    - cron: '0 7 * * 1-5'   # 07:00 UTC = 09:00 CET, weekdays
  workflow_dispatch:          # manual trigger

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

      - uses: actions/setup-python@v5
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests openpyxl

      - name: Run Thesis Pulse
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          FRED_API_KEY:      ${{ secrets.FRED_API }}
          EMAIL_ADDRESS:     ${{ secrets.EMAIL_ADDRESS }}
          EMAIL_PASSWORD:    ${{ secrets.EMAIL_PASSWORD }}
          RECIPIENT_EMAIL:   ${{ secrets.RECIPIENT_EMAIL }}
          WGC_API_AUTH:      ${{ secrets.WGC_API_AUTH }}
          WGC_AUTH_COOKIE:   ${{ secrets.WGC_AUTH_COOKIE }}
          WGC_AUTH_SESSION:  ${{ secrets.WGC_AUTH_SESSION }}
          WGC_XSRF:          ${{ secrets.WGC_XSRF }}
        run: python thesis_pulse.py
